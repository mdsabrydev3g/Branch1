node_modules
*.local

# Server-side secrets — never commit
.env
.env.*
!.env.example

# Local PGLite database (dev-only persistence; production uses Neon)
.pglite-data/

# Vercel build output + local deploy state
.vercel
dist

# QA screenshots
screenshots/

# OS / editor noise
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
